# 任务管理系统 - 团队协作规范
## 一、分支策略（核心准则）
1.  主分支（main）：生产环境专属分支，仅用于发布稳定版本，禁止直接提交、推送代码，仅通过合并dev分支更新。
2.  开发分支（dev）：团队共用开发主分支，保持可运行状态，所有功能分支需合并至此，作为代码集成的中间载体。
3.  功能分支（feature）：每位协作者的专属开发分支，命名格式为`feature/功能模块名`（如`feature/add-update-task`、`feature/query-delete-task`），必须基于dev分支创建，开发完成后合并回dev分支。
4.  禁止行为：严禁直接在main、dev分支编写功能代码，所有开发工作均在feature分支开展。

## 二、提交规范
1.  提交频率：每完成一个独立小功能（如单函数实现、参数校验）或修复一个bug，立即提交代码，避免大量代码堆积，单次提交核心代码不超过50行。
2.  提交信息格式：统一采用`[模块名] 具体描述`，清晰标注修改内容，示例：
    - 正确：`[add-update] 实现任务新增功能，添加空值校验`
    - 正确：`[query-delete] 修复任务删除后ID断层问题`
    - 错误：`修改代码`、`更新文件`（模糊描述不允许）。
3.  提交前检查：确保本地代码可正常运行，无语法错误，再执行暂存、提交操作。

## 三、代码审查要求
1.  合并前提：所有feature分支需通过Pull Request（PR）申请合并至dev分支，禁止直接使用merge命令合并。
2.  审查流程：PR提交后，需由至少1名团队成员（如仓库所有者）审查代码，确认逻辑合理、命名规范、无冗余代码后，方可合并。
3.  审查重点：代码功能完整性、参数校验是否全面、代码可读性（变量/函数命名规范）、是否与现有功能兼容。

## 四、冲突解决原则
1.  预防优先：合并分支前，必须先拉取目标分支（如dev）最新代码，本地同步后再尝试合并，减少冲突概率。
2.  解决规范：冲突发生后，优先在本地解决，保留双方有效代码，不随意删除他人开发的功能模块。
3.  沟通机制：复杂冲突（如核心函数重叠修改）需与对应开发者沟通确认后再解决，解决后需本地自测，确保功能正常再推送。

## 五、附则
1.  代码风格：统一采用Python规范命名（函数用小写下划线，如`add_task`；变量名简洁明意），添加必要注释说明功能。
2.  文件管理：新增功能模块需单独创建文件，文件名与功能对应（如任务新增对应`add_task.py`），避免单文件过度冗余。